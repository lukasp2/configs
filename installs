## Nice to have
sudo apt install -y gcc emacs curl git xclip htop

## Additionals
# sudo apt install -y entr ncdu python3-pip

## Generate ssh key (push and pull from git w/o needing credentials)
ssh-keygen -t rsa
eval $(ssh-agent -s)
ssh-add ~/.ssh/id_rsa

# copy the ssh (you have to paste in into github manually)
xclip -sel clip < ~/.ssh/id_rsa.pub

## Spoitfy
curl -sS https://download.spotify.com/debian/pubkey.gpg | sudo apt-key add - 
echo "deb http://repository.spotify.com stable non-free" | sudo tee /etc/apt/sources.list.d/spotify.list
sudo apt-get update && sudo apt-get install spotify-client
